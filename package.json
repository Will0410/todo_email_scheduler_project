{
  "name": "todo-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "echo Skipped frontend build (Frontend B - build locally)",
    "start": "cd backend && npm install && npm start"
  }
}
